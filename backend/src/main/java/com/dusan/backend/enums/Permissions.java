package com.dusan.backend.enums;

public enum Permissions {
    can_read_users, can_update_users, can_delete_users, can_create_users,
    can_search_order, can_place_order, can_cancel_order,
    can_track_order, can_schedule_order
}
